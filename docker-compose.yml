services:
  csp-agent:
    build: .
    environment:
      - DASHSCOPE_API_KEY=${DASHSCOPE_API_KEY}
      - "BROWSER_ENDPOINT=${BROWSER_ENDPOINT:-ws://ssuzaip38.aptargroup.loc:3000/?token=PW8rJqSTzd&headless=false}"
    volumes:
      - .:/workspace
      - pi-agent-home:/root/.pi/agent
      - ./config/models.json:/root/.pi/agent/models.json:ro
    stdin_open: true
    tty: true

volumes:
  pi-agent-home:
